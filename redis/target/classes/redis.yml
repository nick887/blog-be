spring.redis:
  # 可选single/cluster
  mode: single
  base:
    username: ~
    password: ~
  single:
    address: localhost:6379
    database: 0
    connectionPoolSize: 64
    connectionMinimumIdleSize: 24
  cluster:
    nodes: localhost:6379
